# Svelte + TS + Vite = Web Components

This folder is a builder for web components used in the mbr markdown browser.  The idea is to build stand-alone components that are then compiled into the rust binary, but overridden by components in per-markdown `.mbr` folders.
