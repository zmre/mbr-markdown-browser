{
  "name": "mbr-components",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "watch": "tsc && vite build --watch",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "bench": "vitest bench"
  },
  "dependencies": {
    "lit": "^3.3.2"
  },
  "devDependencies": {
    "@vitest/browser": "^4.1.4",
    "happy-dom": "^20.9.0",
    "terser": "^5.46.1",
    "typescript": "~6.0.3",
    "vite": "^8.0.8",
    "vitest": "^4.1.4"
  }
}
